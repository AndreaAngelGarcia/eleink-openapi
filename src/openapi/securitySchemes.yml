bearerAuth:
    type: http
    scheme: bearer
    bearerFormat: JWT
    description: Token de autenticación JWT con campo "role" igual a "admin".
    name: Authorization
    in: header

security:
  - bearerAuth:
      - role: admin
